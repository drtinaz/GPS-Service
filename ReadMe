This package installs RemoteGPIOv2 for use with TCP over IP. Dingtian module TCP server must be set to TCP over IP
Install Kevin Windrem's package manager and guimods. then add the following to active packages list:
packagename:RemoteGPIOv2
github username:drtinaz
branch:main

Import the dashboard json from this package into node-red, then open the flow and set the IP address accordingly. Can use ethernet or Wifi to connect the relay module. Module sizes 2, 4 or 8 can be used.